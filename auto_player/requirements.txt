# 核心依赖(必装)
pyautogui>=0.9.54       # 鼠标键盘模拟
opencv-python>=4.8.0    # 图像识别(模板匹配)
numpy>=1.24.0           # OpenCV 依赖
mss>=9.0.0              # 高速屏幕截图(比 pyautogui 快很多)
pygetwindow>=0.0.9      # 窗口定位(Windows)
pywin32>=306            # Windows API(pygetwindow 依赖)
PyYAML>=6.0             # 解析规则 yaml

# 可选依赖(OCR 找文字,不用可注释掉)
# pytesseract>=0.3.10   # OCR 文字识别(还需单独安装 Tesseract OCR 引擎)
# pillow>=10.0.0        # 截图备用方案
